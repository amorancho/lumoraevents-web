---
interface Props {
  hero: {
    eyebrow: string;
    title: string;
    description: string;
    primaryCta: string;
    secondaryCta: string;
    imageAlt?: string;
    imageSrc?: string;
    panelLabel: string;
    overlayLabel: string;
    overlayDescription: string;
    placeholderTitle: string;
    placeholderDescription: string;
  };
  primaryHref: string;
  secondaryHref: string;
}

const { hero, primaryHref, secondaryHref } = Astro.props;
const baseUrl = import.meta.env.BASE_URL.replace(/\/$/, "");
const logoSrc = `${baseUrl}/images/logo.svg`;
const heroImageSrc = hero.imageSrc?.trim();
const heroImageAlt = hero.imageAlt ?? hero.placeholderTitle;
---

<section
  id="product"
  class="shell relative pb-16 pt-6 sm:pb-20 sm:pt-10 lg:pb-24"
>
  <div
    class="pointer-events-none absolute inset-x-0 top-2 h-40 bg-[radial-gradient(circle_at_top,rgba(124,58,237,0.18),transparent_68%)] opacity-80"
  >
  </div>
  <div
    class="pointer-events-none absolute right-[12%] top-24 hidden h-24 w-24 rounded-full bg-[radial-gradient(circle,rgba(6,182,212,0.08),transparent_72%)] lg:block"
  >
  </div>

  <div class="relative z-10 max-w-[47rem] lg:max-w-[50rem]">
    <span class="eyebrow reveal">{hero.eyebrow}</span>

    <h1
      class="reveal mt-5 max-w-[11ch] whitespace-pre-line text-5xl font-semibold tracking-[-0.08em] text-white sm:text-6xl lg:text-[6.2rem] lg:leading-[0.92]"
      style="--reveal-delay: 80ms"
    >
      {hero.title}
    </h1>

    <p
      class="reveal mt-5 max-w-[36rem] text-lg leading-8 text-copy-soft sm:text-xl"
      style="--reveal-delay: 160ms"
    >
      {hero.description}
    </p>

    <div
      class="reveal mt-7 flex flex-col gap-3 sm:flex-row"
      style="--reveal-delay: 240ms"
    >
      <a href={primaryHref} class="button-primary px-6 py-3.5">
        {hero.primaryCta}
      </a>
      <a
        href={secondaryHref}
        target="_blank"
        rel="noreferrer"
        class="button-secondary px-6 py-3.5"
      >
        {hero.secondaryCta}
      </a>
    </div>
  </div>

  <div class="relative z-10 mt-10 sm:mt-12">
    <div
      class="pointer-events-none absolute inset-x-[8%] top-5 bottom-5 hidden rounded-[2.6rem] border border-white/6 bg-white/[0.02] lg:block"
    >
    </div>
    <div
      class="pointer-events-none absolute inset-x-[18%] top-0 h-24 bg-[radial-gradient(circle_at_center,rgba(6,182,212,0.07),transparent_72%)]"
    >
    </div>

    <div class="reveal relative mx-auto max-w-[72rem]" style="--reveal-delay: 320ms">
      <div
        class="pointer-events-none absolute -left-2 top-12 hidden h-20 w-20 rounded-full bg-[radial-gradient(circle,rgba(124,58,237,0.12),transparent_72%)] lg:block"
      >
      </div>

      <div
        class="relative overflow-hidden rounded-[2.5rem] border border-white/10 bg-[linear-gradient(180deg,rgba(255,255,255,0.07),rgba(255,255,255,0.02))] p-4 shadow-[0_24px_60px_rgba(0,0,0,0.28)] sm:p-5 lg:p-6"
      >
        <div
          class="pointer-events-none absolute inset-0 bg-[radial-gradient(circle_at_top,rgba(124,58,237,0.1),transparent_34%),radial-gradient(circle_at_82%_18%,rgba(6,182,212,0.05),transparent_20%)]"
        >
        </div>

        <div class="relative flex items-center gap-2 border-b border-white/10 pb-4">
          <span class="size-2 rounded-full bg-white/25"></span>
          <span class="size-2 rounded-full bg-white/20"></span>
          <span class="size-2 rounded-full bg-white/15"></span>
          <span
            class="ml-auto hidden rounded-full border border-white/10 bg-white/[0.04] px-3 py-1 text-[0.64rem] font-semibold uppercase tracking-[0.22em] text-copy-soft sm:inline-flex"
          >
            {hero.panelLabel}
          </span>
        </div>

        <div
          class="relative mt-4 overflow-hidden rounded-[2rem] border border-white/10 bg-black/50 p-3 sm:rounded-[2.1rem] sm:p-4"
        >
          <div
            class="pointer-events-none absolute inset-0 bg-[radial-gradient(circle_at_top,rgba(124,58,237,0.12),transparent_34%),linear-gradient(180deg,rgba(255,255,255,0.02),transparent_26%,rgba(255,255,255,0.015)_100%)]"
          >
          </div>

          {
            heroImageSrc ? (
              <div class="relative overflow-hidden rounded-[1.5rem] border border-white/8 bg-black/30">
                <img
                  src={heroImageSrc}
                  alt={heroImageAlt}
                  class="h-auto w-full rounded-[1.5rem] object-cover"
                  loading="eager"
                  fetchpriority="high"
                  decoding="async"
                />
              </div>
            ) : (
              <div class="relative min-h-[18rem] rounded-[1.5rem] border border-white/8 bg-[linear-gradient(180deg,rgba(20,20,24,0.92),rgba(8,8,9,0.98))] sm:min-h-[24rem] lg:min-h-[30rem]">
                <div
                  class="pointer-events-none absolute inset-0 bg-[radial-gradient(circle_at_top,rgba(124,58,237,0.14),transparent_42%),radial-gradient(circle_at_80%_18%,rgba(6,182,212,0.08),transparent_24%)]"
                >
                </div>
                <div
                  class="pointer-events-none absolute inset-y-0 left-[11%] hidden w-px bg-white/6 lg:block"
                >
                </div>
                <div
                  class="pointer-events-none absolute inset-x-0 bottom-0 h-32 bg-[linear-gradient(180deg,transparent,rgba(5,5,6,0.82))]"
                >
                </div>

                <div class="relative flex h-full flex-col justify-between">
                  <div class="flex items-center justify-between px-5 py-5 sm:px-7">
                    <img
                      src={logoSrc}
                      alt="LumoraEvents"
                      class="h-7 w-auto opacity-95"
                      loading="eager"
                    />
                    <span
                      class="rounded-full border border-white/10 bg-white/[0.04] px-3 py-1 text-[0.64rem] font-semibold uppercase tracking-[0.22em] text-copy-soft sm:hidden"
                    >
                      {hero.panelLabel}
                    </span>
                  </div>

                  <div class="max-w-lg px-5 pb-6 sm:px-7 sm:pb-8 lg:px-8 lg:pb-10">
                    <p
                      class="text-[0.68rem] font-semibold uppercase tracking-[0.28em] text-copy-soft/70"
                    >
                      {hero.overlayLabel}
                    </p>
                    <p
                      class="mt-4 text-2xl font-semibold tracking-[-0.05em] text-white sm:text-3xl"
                    >
                      {hero.placeholderTitle}
                    </p>
                    <p
                      class="mt-3 max-w-md text-sm leading-7 text-copy-soft sm:text-base"
                    >
                      {hero.placeholderDescription}
                    </p>
                  </div>
                </div>
              </div>
            )
          }
        </div>
      </div>

      <div
        class="absolute right-3 top-[-1rem] z-20 hidden w-[12rem] rounded-[1.3rem] border border-white/10 bg-black/78 px-4 py-3 shadow-[0_16px_36px_rgba(0,0,0,0.24)] sm:block lg:right-8 lg:top-[-1.2rem] lg:w-[13rem]"
      >
        <span
          class="inline-flex items-center gap-2 text-[0.62rem] font-semibold uppercase tracking-[0.24em] text-copy-soft/80"
        >
          <span class="size-2 rounded-full bg-accent-cyan"></span>
          {hero.overlayLabel}
        </span>
        <p class="mt-3 text-sm leading-6 text-white/92">
          {hero.overlayDescription}
        </p>
      </div>
    </div>
  </div>
</section>
